{
  "name": "basic-auth",
  "version": "1.0.0",
  "description": "Lambda@Edge for basic auth.",
  "main": "app.js",
  "author": "Canji",
  "license": "MIT",
  "jest": {
    "setupFiles": [
      "dotenv/config"
    ]
  },
  "scripts": {
    "test": "jest",
    "test watch": "jest --watch"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "aws-lambda-mock-context": "^3.2.1",
    "jest": "^26.5.3"
  }
}
